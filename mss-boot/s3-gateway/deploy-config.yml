project: mss-boot
service: s3-gateway
ports:
  - port: 8080
    targetPort: 8080
    name: http
metrics:
  scrape: true
  port: 5000
  path: /metrics
hpa: false
config:
  - name: mss-boot-s3-gateway-config
    path: /app/cfg
deploy:
  image: mssbootio/s3-gateway
  stage:
    alpha:
      cluster: matrix-devops
      namespace: alpha
      autoSync: true
    beta:
      cluster: matrix-devops
      namespace: beta
    prod:
      cluster: matrix-devops
      namespace: prod